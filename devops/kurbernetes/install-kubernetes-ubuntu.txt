> apt-get update && apt-get install -y apt-transport-https

> curl -s https://packages.cloud.goolge.com/apt/doc/apt-key.gpg | apt-key add -
cat <<EOF>/etc/apt/sources.list.d/kubernetes.list
deb http://apt.kubernetes.io/ kubernetes-xenial main
EOF

> apt-get update

> apt-get instal -y docker.io kubeadm kubectl kubelet kubernetes-cni

# this command will run as normal 'ubuntu' user not at 'root' user.
# and it applies only for master not for minios-nodes machines, I mean below all commands
# run on only at master
> kubeadm init 

> kubectl get nodes

> kubectl get pods --all-namespaces

> kubectl apply --filename https://git.io/weave-kube-1.6

> kubectl get nodes

> kubectl get pods --all-namespaces

> kubectl get nodes

--------------------------------
